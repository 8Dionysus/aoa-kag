# Antifragility Legacy

Antifragility legacy records former root paths and historical naming for
projection-health, quarantine, and retrieval-outage regrounding payloads.

Use `INDEX.md` for path accounting and `DISTILLATION_LOG.md` for why active
payloads moved.
