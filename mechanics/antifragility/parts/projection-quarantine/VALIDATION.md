# Projection Quarantine Validation

Run:

```bash
python -m unittest discover -s mechanics/antifragility/parts/projection-quarantine/tests -p 'test_*.py'
```

Then run:

```bash
python scripts/validate_mechanics_skeleton.py
python scripts/run_tests.py
```
