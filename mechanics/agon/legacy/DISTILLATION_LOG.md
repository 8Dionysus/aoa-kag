# Agon Legacy Distillation Log

## 2026-06-19

- Activated `promotion-candidates` and `sophian-threshold-packets` as Agon
  part-local KAG routes.
- Moved former root configs, builders, validators, focused tests, and short
  Agon docs into the owning part directories.
- Replaced active `seed` path naming with `.source.json` part-local configs.
- Replaced active `wave` and `wave_name` payload keys with `review_stage` and
  `review_stage_label`.
- Replaced former landing doc path names with functional active route notes and
  retired old Agon quest sidecars from the active quest store.
- Kept generated registries root-published under `generated/`.
