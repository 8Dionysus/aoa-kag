# ToS Retrieval Axis Validation

Run:

```bash
python -m unittest discover -s mechanics/boundary-bridge/parts/tos-retrieval-axis/tests -p 'test_*.py'
```

For generated-output changes, also run the generated lane and release lane.
