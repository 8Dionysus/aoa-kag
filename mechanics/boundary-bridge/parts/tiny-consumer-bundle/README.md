# Tiny Consumer Bundle

## Mechanic card

Status: active Boundary Bridge part for the current tiny consumer bundle.

### Trigger

Use this part when the bounded consumer chain, tiny bundle manifest, generated
bundle, schemas, example, or deferred counterpart posture changes.

### Local owns

The part owns the manifest, schemas, example, and generated tiny consumer
bundle that package the current reviewable consumer path.

### Stronger owner split

Source-owning parts own their generated surfaces. `counterpart-edge` owns
counterpart contract refs. `audit/exposure-review` owns exposure review.
`aoa-routing` does not inherit routing authority from this bundle.

### Inputs

Current generated bridge surfaces, `docs/CONSUMER_GUIDE.md`, counterpart-edge
contract refs, and exposure-review refs.

### Outputs

Part-local generated `tiny_consumer_bundle*.json` plus schema/example parity.

### Must not claim

Counterpart activation, route dispatch, source replacement, hidden graph
sovereignty, consumer ranking, or release approval.

### Next route

Change `manifests/tiny_consumer_bundle.json`, regenerate with
`python scripts/generate_kag.py`, then validate with `python scripts/validate_kag.py`.
