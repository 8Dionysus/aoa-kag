# Experience Legacy

Experience legacy records former root paths and historical naming for
Experience KAG surfaces that now route through active part-local homes.

It is not active topology and not a new-work entrypoint.

## Layout

- `INDEX.md`: former root paths and names mapped to active routes.
- `DISTILLATION_LOG.md`: dated accounting for active-route distillation.
- `AGENTS.md`: local route law for legacy edits.
