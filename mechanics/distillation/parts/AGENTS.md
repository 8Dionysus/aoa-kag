# Distillation Parts Agents

## Applies to

This card applies to `mechanics/distillation/parts/` and all active
distillation part descendants.

## Role

Distillation parts own bounded KAG lift routes from stronger source surfaces
into local derived payload families.

## Read before editing

Read the package `AGENTS.md`, `README.md`, `PARTS.md`, `PROVENANCE.md`, and the
nearest part `CONTRACT.md`.

## Boundaries

Keep source meaning with the source repository. Repo-wide manifests, schemas,
examples, generated outputs, and compatibility builders stay in their repo-wide
districts unless the part contract explicitly moves them.

## Validation

Run the nearest part tests and then the mechanics skeleton validator. Generated
parity still runs through the repo-wide generated lane.

## Closeout

Name the lift route touched, stronger source preserved, focused tests run, and
whether generated parity or release checks were needed.
