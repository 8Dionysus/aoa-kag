# Release Support Parts

Active KAG release-support parts:

| Part | Owns |
| --- | --- |
| `release-lane` | release command posture, lane-manifest delegation, and release entrypoint contract |

The lane manifest and public scripts remain root-owned command and entrypoint
surfaces. This part owns the mechanics contract around their release-support
posture.
