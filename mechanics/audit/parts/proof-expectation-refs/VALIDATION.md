# Proof Expectation Refs Validation

Run:

```bash
python -m unittest discover -s mechanics/audit/parts/proof-expectation-refs/tests -p 'test_*.py'
```

For generated-output changes, also run the generated lane and release lane.
