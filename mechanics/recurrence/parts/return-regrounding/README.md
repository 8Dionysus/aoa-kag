# Return Regrounding

## Mechanic card

Status: active Recurrence part for KAG return-regrounding packs.

### Trigger

Use this part when return-regrounding docs, manifests, generated packs,
recurrence projection companions, stronger refs, reentry modes, or
owner-boundary return posture changes.

### Local owns

The part owns focused validation and tests that keep return-regrounding packs
as derived routes back to stronger refs instead of route, memory, proof, or
canon ownership.

### Stronger owner split

`aoa-routing` owns live route behavior. `aoa-memo` owns memory truth and
readiness. `aoa-evals` owns proof. Source repositories own source meaning and
source-owned exports. Runtime owners own recovery execution.

### Inputs

Return-regrounding docs, manifest, recurrence projection companion, generated
pack, source-owned export refs, reasoning handoff refs, projection refs, and
stronger owner boundary refs.

### Outputs

Part-local generated return-regrounding pack plus validation result and
source-route failure messages.

### Must not claim

Live recurrence activation, memory truth, proof verdict, route dispatch,
runtime recovery, source rewrite, direct canon authorship, or hidden graph
repair.

### Next route

Change the return-regrounding source doc or manifest first, regenerate when
needed, then run this part's validator. Repo-wide generation and validation
remain compatibility entrypoints that cross-check the part-local surfaces.
