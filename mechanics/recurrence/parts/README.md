# Recurrence Parts

`mechanics/recurrence/parts/` holds active KAG-local recurrence sub-mechanics.

Use this home for functioning return routes with local contracts and
validation. Source docs, manifests, schemas, examples, and generated read
models may stay in their root homes while part-local validation owns the
operation contract around them.

## Active parts

| Part | Route |
| --- | --- |
| `return-regrounding` | validates return-regrounding mode order, stronger-ref stop-lines, memo readiness ownership, and derived return contracts |
