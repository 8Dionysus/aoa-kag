# Suites

Future deterministic KAG suites may live here when they preserve local
projection, retrieval, or regrounding fixtures.

Central verdict, scoring, regression, and proof doctrine routes stay in
`aoa-evals`.
