# AGENTS.md

Local route card for `aoa-kag/evals/`.

This skeleton port captures future KAG eval pressure while preserving the rule
that KAG is derived, not proof.

`aoa-evals` owns central verdict, scoring, regression, and proof doctrine
authority. Keep retrieval and projection source evidence here; route proof
adoption to `aoa-evals`.

Validation:

```bash
python ../aoa-evals/scripts/validate_local_eval_port.py --target-root .
```
